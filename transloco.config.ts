import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'projects/docs/public/assets/i18n/',
  langs: ['en', 'ru', 'ro'],
  keysManager: {},
};

export default config;
