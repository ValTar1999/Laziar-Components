export interface LzAvatarGroupItem {
  imgUrl?: string;
  firstName: string;
  lastName: string;
}

/** Size scale — includes `xxs` from publikator. */
export type LzAvatarGroupSize = 'xxs' | 'xs' | 'sm' | 'md' | 'lg';
