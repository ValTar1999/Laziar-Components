export type LzTabButtonSize = 'sm' | 'md' | 'lg';

/**
 * `solid` / `bg` = filled active background (F+P use `bg`);
 * `line` = underline indicator.
 */
export type LzTabButtonVariant = 'solid' | 'bg' | 'line';
